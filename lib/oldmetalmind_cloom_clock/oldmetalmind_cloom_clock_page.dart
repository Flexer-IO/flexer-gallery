import 'package:flutter/material.dart';
import 'main.dart';

class OldmetalmindCloomClockPage extends StatelessWidget {
  const OldmetalmindCloomClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    // The library's main entry widget is defined in `main.dart`.
    // It is typically named `Clock`. If the actual widget has a different
    // name, replace `Clock` with the correct class name from the library.
    return const Clock();
  }
}
