import 'package:flutter/material.dart';
import 'ui/home.dart';

class JideguruSalesappPage extends StatelessWidget {
  const JideguruSalesappPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Home(
      header: 'Sales UI',
    );
  }
}