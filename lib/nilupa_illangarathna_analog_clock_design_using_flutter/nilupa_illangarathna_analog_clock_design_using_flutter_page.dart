import 'package:flutter/material.dart';

class NilupaIllangarathnaAnalogClockDesignUsingFlutterPage extends StatelessWidget {
  const NilupaIllangarathnaAnalogClockDesignUsingFlutterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const HomePage();
  }
}

// Stub implementation of HomePage to resolve missing import.
class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}