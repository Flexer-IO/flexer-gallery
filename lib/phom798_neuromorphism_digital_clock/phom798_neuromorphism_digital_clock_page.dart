import 'package:flutter/material.dart';
import 'neuromorphic_digital_clock.dart';

class Phom798NeuromorphismDigitalClockPage extends StatelessWidget {
  const Phom798NeuromorphismDigitalClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    // The library's primary clock widget is assumed to be NeuromorphicDigitalClock.
    // It is imported from the library's main.dart file.
    return const NeuromorphicDigitalClock();
  }
}