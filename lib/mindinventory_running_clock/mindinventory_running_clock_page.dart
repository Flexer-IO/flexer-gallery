import 'package:flutter/material.dart';
import '../main.dart';

class MindinventoryRunningClockPage extends StatelessWidget {
  const MindinventoryRunningClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    // The library's main entry widget is defined in `main.dart`.
    // It is typically named `MyApp` (the default widget that sets up the clock).
    // Adjust the widget name if the library uses a different identifier.
    return const MyApp();
  }
}
