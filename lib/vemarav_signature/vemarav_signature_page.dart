import 'package:flutter/material.dart';
import '../signature.dart';

class VemaravSignaturePage extends StatelessWidget {
  const VemaravSignaturePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Signature(),
    );
  }
}
