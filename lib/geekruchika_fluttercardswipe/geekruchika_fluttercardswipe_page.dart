import 'package:flutter/material.dart';
import 'main.dart';

class GeekruchikaFluttercardswipePage extends StatelessWidget {
  const GeekruchikaFluttercardswipePage({super.key});

  @override
  Widget build(BuildContext context) {
    // The library's main entry widget is defined in its `main.dart`.
    // We simply return that widget so the original demo experience is shown.
    return MyApp();
  }
}