import 'package:flutter/material.dart';
import 'package:clock_challenge/main.dart';

class RomanejaquezFlutterClockChallengePage extends StatelessWidget {
  const RomanejaquezFlutterClockChallengePage({super.key});

  @override
  Widget build(BuildContext context) {
    // The library's main entry widget is defined in its `main.dart`.
    // It already sets up any required providers and routing.
    return const ClockApp();
  }
}
