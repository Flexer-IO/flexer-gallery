import 'package:flutter/material.dart';
import 'taichi_clock.dart';

class XuyishengTaichiClockPage extends StatelessWidget {
  const XuyishengTaichiClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ClockWidget(),
    );
  }
}