import 'package:flutter/material.dart';
import 'clock/clock_view.dart';

class RenatolucasmotaAnalogClockFlutterPage extends StatelessWidget {
  const RenatolucasmotaAnalogClockFlutterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ClockView(),
    );
  }
}