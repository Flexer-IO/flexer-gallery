import 'package:flutter/material.dart';
import 'analog_clock.dart';

class EmilysmithsonAnalogClockPage extends StatelessWidget {
  const EmilysmithsonAnalogClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AnalogClock(),
    );
  }
}