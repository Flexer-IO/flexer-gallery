import 'package:flutter/material.dart';
import 'main.dart';

class MartinoyovoFlutterDesignCollectionPage extends StatelessWidget {
  const MartinoyovoFlutterDesignCollectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    // The library's main entry widget is defined in `main.dart`.
    // It is instantiated here without any additional configuration.
    return MyApp();
  }
}
