import 'package:flutter/material.dart';
import '../clock.dart';

class Sbis04BinaryClockPage extends StatelessWidget {
  const Sbis04BinaryClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Clock(),
    );
  }
}
