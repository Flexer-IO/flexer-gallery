import 'package:flutter/material.dart';
import '../clock.dart';

class GermainLHecksclockPage extends StatelessWidget {
  const GermainLHecksclockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Clock(),
    );
  }
}
