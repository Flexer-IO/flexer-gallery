import 'package:flutter/material.dart';
import '../main.dart';

class GustaviusssPomodoroClockFlutterPage extends StatelessWidget {
  const GustaviusssPomodoroClockFlutterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Pomodoro(),
    );
  }
}
