import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder implementation; replace with actual HomeScreen UI.
    return const SizedBox.shrink();
  }
}

class CodewithdarkwaDigitalAlarmClockDesignPage extends StatelessWidget {
  const CodewithdarkwaDigitalAlarmClockDesignPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const HomeScreen();
  }
}