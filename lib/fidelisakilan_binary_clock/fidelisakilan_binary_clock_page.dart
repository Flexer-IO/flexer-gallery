import 'package:flutter/material.dart';
import '../main.dart';

class FidelisakilanBinaryClockPage extends StatelessWidget {
  const FidelisakilanBinaryClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Clock(),
    );
  }
}
