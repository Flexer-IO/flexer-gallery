import 'package:flutter/material.dart';
import 'main.dart';

class MdudzinskiFlutterClockPage extends StatelessWidget {
  const MdudzinskiFlutterClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    // The library's entry widget is defined in `main.dart`.
    // It already sets up the necessary model and UI.
    return const MyApp();
  }
}
