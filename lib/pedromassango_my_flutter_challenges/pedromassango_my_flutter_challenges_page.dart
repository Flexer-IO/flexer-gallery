import 'package:flutter/material.dart';
import '../main.dart';

class PedromassangoMyFlutterChallengesPage extends StatelessWidget {
  const PedromassangoMyFlutterChallengesPage({super.key});

  @override
  Widget build(BuildContext context) {
    // The library's entry point widget is defined in main.dart.
    // We simply return that widget so the full library experience is displayed.
    return MyApp();
  }
}
