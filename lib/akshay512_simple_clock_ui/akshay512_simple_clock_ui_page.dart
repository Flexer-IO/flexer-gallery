import 'package:flutter/material.dart';
import '../background.dart';

class Akshay512SimpleClockUiPage extends StatelessWidget {
  const Akshay512SimpleClockUiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: const Center(
        child: Background(),
      ),
    );
  }
}
