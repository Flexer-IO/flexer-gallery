name: showcase_library
resolution: workspace
description: "Flexer public showcase library — community UI masterclasses"
publish_to: none
environment:
  sdk: ^3.12.0

dependencies:
  intl:
    path: lib/chhannan_flutter_clock_challenge/deps/intl
  clock:
    path:
    path: lib/chhannan_flutter_clock_challenge/deps/path
  flare_flutter:
    path:
    path: lib/chhannan_flutter_clock_challenge/deps/path
  collection:
    path:
    path: lib/chhannan_flutter_clock_challenge/deps/path
  meta:
    path:
    path: lib/chhannan_flutter_clock_challenge/deps/path
  flutter_clock_helper:
    path:
    path: lib/chhannan_flutter_clock_challenge/deps/path
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

flutter:
  assets:
    - assets/miickel_flutter_particle_clock/palettes.json
  uses-material-design: true
