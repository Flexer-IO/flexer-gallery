name: showcase_library
resolution: workspace
description: "Flexer public showcase library — community UI masterclasses"
publish_to: none
environment:
  sdk: ^3.12.0

dependencies:
  intl: ^0.20.2
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  auto_size_text: ^3.0.0
  simple_animations: ^1.3.3
flutter:
  assets:
    - assets/afgprogrammer_flutter_job_list_application/jobs.json
    - assets/miickel_flutter_particle_clock/palettes.json
  uses-material-design: true


