name: showcase_library
resolution: workspace
description: "Flexer public showcase library — community UI masterclasses"
publish_to: none
environment:
  sdk: ^3.12.0

dependencies:
  material_design_icons_flutter:
    path: lib/imkcat_catclock/deps/material_design_icons_flutter
  intl:
    path: lib/imkcat_catclock/deps/intl
  clock:
    path:
    path: lib/imkcat_catclock/deps/path
  flare_flutter:
    path:
    path: lib/imkcat_catclock/deps/path
  collection:
    path:
    path: lib/imkcat_catclock/deps/path
  meta:
    path:
    path: lib/imkcat_catclock/deps/path
  flutter_clock_helper:
    path:
    path: lib/imkcat_catclock/deps/path
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

flutter:
  assets:
    - assets/miickel_flutter_particle_clock/palettes.json
  uses-material-design: true
