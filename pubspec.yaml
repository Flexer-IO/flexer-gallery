name: showcase_library
description: "Flexer public showcase library — community UI masterclasses"
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  flutter_clock_helper: any
flutter:
  uses-material-design: true

