name: showcase_library
resolution: workspace
description: "Flexer public showcase library — community UI masterclasses"
publish_to: none
environment:
  sdk: ^3.12.0

dependencies:
  intl:
    path: lib/isatippens_flutter_clock/deps/intl
  clock:
    path:
    path: lib/isatippens_flutter_clock/deps/path
  meta:
    path:
    path: lib/isatippens_flutter_clock/deps/path
  flutter_clock_helper:
    path:
    path: lib/isatippens_flutter_clock/deps/path
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

  auto_size_text: ^3.0.0
  intl: ^0.20.2
flutter:
  assets:
    - assets/miickel_flutter_particle_clock/palettes.json
  uses-material-design: true

