name: showcase_library
resolution: workspace
description: "Flexer public showcase library — community UI masterclasses"
publish_to: none
environment:
  sdk: ^3.12.0

dependencies:
  flutter_weather_icons:
    path: lib/salibhdr_typhoon_flutter_clock/deps/flutter_weather_icons
  intl:
    path: lib/salibhdr_typhoon_flutter_clock/deps/intl
  clock:
    path:
    path: lib/salibhdr_typhoon_flutter_clock/deps/path
  meta:
    path:
    path: lib/salibhdr_typhoon_flutter_clock/deps/path
  flutter_clock_helper:
    path:
    path: lib/salibhdr_typhoon_flutter_clock/deps/path
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

flutter:
  assets:
    - assets/miickel_flutter_particle_clock/palettes.json
  uses-material-design: true
