name: showcase_library
resolution: workspace
description: "Flexer public showcase library — community UI masterclasses"
publish_to: none
environment:
  sdk: ^3.12.0

dependencies:
  provider:
    path: lib/fluttercandies_flutter_custom_calendar/deps/provider
  nested:
    path: lib/fluttercandies_flutter_custom_calendar/deps/nested
  collection:
    path: lib/fluttercandies_flutter_custom_calendar/deps/collection
  intl: ^0.20.2
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  auto_size_text: ^3.0.0
flutter:
  assets:
    - assets/miickel_flutter_particle_clock/palettes.json
  uses-material-design: true

