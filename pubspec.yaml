name: showcase_library
resolution: workspace
description: "Flexer public showcase library — community UI masterclasses"
publish_to: none
environment:
  sdk: ^3.12.0

dependencies:
  carousel_slider:
    path: lib/whatsupcoders_flutter_nested_tabs/deps/carousel_slider
  font_awesome_flutter:
    path: lib/whatsupcoders_flutter_nested_tabs/deps/font_awesome_flutter
  intl: ^0.20.2
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  auto_size_text: ^3.0.0
flutter:
  assets:
    - assets/miickel_flutter_particle_clock/palettes.json
  uses-material-design: true

